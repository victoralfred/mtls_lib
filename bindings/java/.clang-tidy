# clang-tidy configuration for Java JNI bindings
#
# JNI code uses naming conventions and patterns that don't match the project's
# C style guide but are required by the JNI specification (e.g., JNICALL,
# jbyteArray, jintArray). This configuration disables checks that conflict
# with standard JNI patterns while maintaining essential safety checks.

Checks: >
  -*,
  bugprone-*,
  -bugprone-multi-level-implicit-pointer-conversion,
  -bugprone-easily-swappable-parameters,
  clang-analyzer-*,
  -clang-diagnostic-error,
  performance-*,
  -performance-no-int-to-ptr,
  readability-braces-around-statements,
  readability-simplify-boolean-expr,
  cppcoreguidelines-*,
  -cppcoreguidelines-avoid-magic-numbers,
  -cppcoreguidelines-pro-type-cstyle-cast,
  -cppcoreguidelines-init-variables,
  -cppcoreguidelines-avoid-non-const-global-variables,
  -misc-include-cleaner

CheckOptions:
  - key: readability-function-cognitive-complexity.Threshold
    value: 50
  - key: readability-function-size.LineThreshold
    value: 200

# Suppress warnings from JNI headers
HeaderFilterRegex: '^.*mtls.*$'
WarningsAsErrors: ''
